# Backspace String Compare:
# Given two strings S and T, return if they are equal when both are typed into empty text editors. # means a backspace character.

# Example:
# Input: S = "ab#c", T = "ad#c"
# Output: true (Both S and T become "ac").

# Example:
# Input: S = "ab##c", T = "ad#c"
# Output: False (S ="c" and T become "ac").

#We need to know about the STACK Data Structure.